# Candidate Assessment Report

| Field | Value |
|-------|-------|
| **Candidate** | {{ candidate or "Not specified" }} |
| **Repository** | {{ repo_url or repo_data.path }} |
| **Date** | {{ date }} |
| **Files Analyzed** | {{ repo_data.total_files }} |
| **Total Lines** | {{ repo_data.total_lines }} |
| **Languages** | {{ repo_data.languages.keys() | join(", ") }} |

---

## Automated Scan (Layer 1)

### Test Coverage

| Metric | Value |
|--------|-------|
| Test-to-Source Ratio | {{ "%.0f" | format(scan_result.test_ratio * 100) }}% |
| Test Files Found | {{ test_file_count }} |
| Source Files | {{ source_file_count }} |

### Linter / Formatter Configuration

{% if scan_result.has_linter_config -%}
Configs found: {{ scan_result.linter_configs_found | join(", ") }}
{% else -%}
**No linter/formatter configuration detected.**
{% endif %}

### Commit History

| Metric | Value |
|--------|-------|
| Total Commits | {{ scan_result.commit_count }} |
| Commit Quality | {{ scan_result.commit_quality | upper }} |

{% if scan_result.commit_count <= 3 -%}
> **Warning**: Low commit count ({{ scan_result.commit_count }}). This may indicate a code dump rather than iterative development.
{% endif %}

Recent commits:
{% for msg in scan_result.commit_messages[:10] %}
- `{{ msg }}`
{% endfor %}

### Dependencies

{% if scan_result.dependencies -%}
{% for dep in scan_result.dependencies -%}
- {{ dep }}
{% endfor %}
{% else -%}
No dependency files detected.
{% endif %}

### Error Handling

Pattern detected: **{{ scan_result.error_handling | upper }}**

{% if scan_result.error_handling == "blanket" -%}
> **Flag**: Blanket error handling detected (bare except / catch-all patterns). This may indicate unrefined AI-generated code.
{% elif scan_result.error_handling == "minimal" -%}
> **Flag**: Minimal error handling detected. Critical paths may be unprotected.
{% endif %}

### Security Flags

{% if scan_result.security_flags -%}
{% for flag in scan_result.security_flags -%}
- {{ flag }}
{% endfor %}
{% else -%}
No security issues detected.
{% endif %}

---

## AI Expert Review (Layer 2)

### Architecture Awareness — {{ review_result.architecture_awareness.score }}/5

{{ review_result.architecture_awareness.justification }}

{% if review_result.architecture_awareness.evidence -%}
**Evidence:**
{% for e in review_result.architecture_awareness.evidence -%}
- `{{ e }}`
{% endfor %}
{% endif %}

### Code Refinement — {{ review_result.code_refinement.score }}/5

{{ review_result.code_refinement.justification }}

{% if review_result.code_refinement.evidence -%}
**Evidence:**
{% for e in review_result.code_refinement.evidence -%}
- `{{ e }}`
{% endfor %}
{% endif %}

### Edge Case Coverage — {{ review_result.edge_case_coverage.score }}/5

{{ review_result.edge_case_coverage.justification }}

{% if review_result.edge_case_coverage.evidence -%}
**Evidence:**
{% for e in review_result.edge_case_coverage.evidence -%}
- `{{ e }}`
{% endfor %}
{% endif %}

### Overall Summary

{{ review_result.overall_summary }}

---

## Classification

| Metric | Value |
|--------|-------|
| **Average Pillar Score** | {{ "%.1f" | format(classification.average_score) }} / 5 |
| **Classification** | **{{ classification.label }}** |
| **Recommendation** | **{{ classification.recommendation }}** |

---

*Generated by [The Driver](https://github.com/the-driver) v0.1.0*
